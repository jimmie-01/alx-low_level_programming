#include "main.h"
/**
 * mul - Multiply two integers
 * @x: The first integer
 * @y: The second integer
 * Return: The result from multiplying two integers
 */
int mul(int x, int y)
{
	return (x * y);
}
