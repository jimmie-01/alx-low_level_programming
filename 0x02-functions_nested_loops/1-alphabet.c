#include "main.h"
/**
 * main - Print alphabet in lowercase followed by new line.
 * Return: void
 * Decription:Prints lowercase alphabet
 */
void print_alphabet(void)
{
	char c;

	for
		(c = 'a';
		 c <= 'z';
		 c++)
		{
			_putchar(c);
		}
	_putchar('\n');
}
